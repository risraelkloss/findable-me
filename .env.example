# ============================================
# Admin Authentication (server-side only)
# ============================================
FINDABLE_ADMIN_PASSWORD=your-secure-password-here
FINDABLE_JWT_SECRET=your-jwt-secret-here

# ============================================
# Resend Email API (server-side only)
# ============================================
RESEND_API_KEY=re_xxxxxxxxxxxx
RESEND_FROM_EMAIL=hello@findable.me
CONTACT_TO_EMAIL=israel@findable.me

# ============================================
# n8n Workflow Automation (server-side only)
# ============================================
N8N_WEBHOOK_URL=https://findableconsulting.app.n8n.cloud/webhook/contact-form

# ============================================
# Notes:
# - Copy this file to .env.local and fill in real values
# - .env.local is git-ignored (via *.local in .gitignore)
# - Server-side vars (no VITE_ prefix) are only available
#   in Vercel serverless functions (/api/*), NOT in the browser
# - Set these same values in Vercel Project Settings > Environment Variables
# ============================================
